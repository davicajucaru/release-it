on:
  push:
    branches:
      - main
name: release-it
jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: npm install
      - run: npm run release
        env:
          GITHUB_TOKEN: ${{ secrets.TOKEN }}
